[package]
name = "dx-route-dedup"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Session dedup — cross-turn exact and fuzzy text deduplication"

[dependencies]
thiserror.workspace = true
tracing.workspace = true
sha2 = "0.10"
hex = "0.4"
