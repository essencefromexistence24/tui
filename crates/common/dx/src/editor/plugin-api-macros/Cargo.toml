[package]
name = "dx-plugin-api-macros"
version = "1.0.0"
edition = "2021"
description = "Proc macros for Dx plugin API type-safe bindings"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "parsing", "extra-traits"] }
